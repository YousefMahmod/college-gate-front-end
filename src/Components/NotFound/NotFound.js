import styles from './NotFound.module.css'

const NotFound = () => {
    return ( 
        <div className= {styles.not_found}>
            <h2>Not Found</h2>
            {/* link */}
        </div>
    );
}
 
export default NotFound;